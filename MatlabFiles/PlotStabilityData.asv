function [ plot ] = PlotStabilityData( trialnames, yvals, means, stddevs )
%PlotStabilityData
%   Detailed explanation goes here
f2 = figure();

scatter(yvals, means);
hold on;
scatter(yvals, stddevs);
scater(yvals, stddevs - means);


end

